---
declared-services:
  discovery-service:
    label: discovery
    plan: free
  conversation-service:
    label: conversation
    plan: free
applications:
- services:
  - discovery-service
  - conversation-service
  path: build/libs/conversation-enhanced-0.1-SNAPSHOT.war
  memory: 512M
